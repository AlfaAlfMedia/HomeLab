# .env.example
# API-Token für Hetzner DNS (ersetze den Platzhalter durch deinen echten Token)
HETZNER_DNS_TOKEN=your_hetzner_dns_token_here

# Domain, für die das Zertifikat erstellt werden soll (ersetze "example.com" mit deiner Domain)
DOMAIN=example.com

# E-Mail-Adresse für Certbot (wird für Benachrichtigungen und als Kontaktadresse verwendet)
EMAIL=your.email@example.com

# Staging-Modus: Setze auf 1, um den Let's Encrypt Staging-Modus zu nutzen (für Tests)
STAGING=1
